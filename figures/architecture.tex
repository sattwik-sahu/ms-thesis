\begin{tikzpicture}[
		node distance=1.2cm and 2.0cm,
		font=\sffamily\small,
		% Professional Grayscale Styles
		block/.style={rectangle, draw=black!80, thick, minimum width=1.8cm, minimum height=1cm, align=center, fill=white, rounded corners=2pt},
		loss/.style={block, fill=OrangeRed!5, draw=OrangeRed, minimum width=2.2cm},
		pred_style/.style={block, fill=Dandelion!5, draw=Dandelion},
		enc_style/.style={block, fill=Cerulean!5, draw=Cerulean},
		input/.style={font=\sffamily\bfseries, text=black},
		arrow/.style={-Stealth, thick, draw=black!70, line width=0.8pt},
		% Style for the EBM container
		ebm_box/.style={draw=Emerald, dashed, line width=1.2pt, fill=gray!5, fill opacity=0.4, rounded corners=5pt}
	]

	% --- Top Branch (Regularization) ---
	\node [block, label={[font=\bfseries, text=black!80]}] (buffer) {$\mathcal{B}$};
	\node [enc_style, right=of buffer] (reg_enc) {$g_\theta$};
	\node [loss, right=of reg_enc] (sig_loss) {$\mathcal{L}_\mathrm{SIGReg}$};
	\node[above=-0.1cm of sig_loss, font=\scriptsize, text=Gray!75]{Regularization};

	% --- Middle Branch (Prediction) ---
	% Position enc_t below the top branch
	\node[enc_style, below=1.5cm of reg_enc] (enc_t) {$g_\theta$};
	\node[pred_style, right=of enc_t] (pred) {${h_\theta}$};
	\node[loss, right=of pred] (l2_loss) {$\mathcal{L}_{\mathrm{pred}}$};

	% --- Bottom Branch (Target) ---
	\node [enc_style, below=1.5cm of pred] (enc_tp1) {$g_\theta$};

	% --- Inputs (Vertically Aligned) ---
	% ot aligned with enc_t
	\node[input, left=2.6cm of enc_t] (ot) {$o_t$};
	% ot+1 forced to align vertically with ot
	\node [input] at (ot |- enc_tp1) (otp1) {$o_{t+1}$};
	\node [input, above=0.6cm of pred] (z) {$\mathbf{z}$};

	% --- EBM f_theta Container ---
	% This box wraps the components that make up the EBM
	\begin{scope}[on background layer]
		\node[ebm_box, fit=(enc_t) (enc_tp1) (l2_loss) (pred), label={[font=\bfseries, text=black!70]below:EBM $f_\theta$}] (ebm_container) {};
	\end{scope}

	% --- Connections ---

	% Top path
	\draw [arrow] (buffer) -- node[above, font=\scriptsize] {$b_0 \sim \mathcal{B}$} (reg_enc);
	\draw [arrow] (reg_enc) -- (sig_loss);

	% Middle path
	\draw [arrow] (ot) -- (enc_t);
	\draw [arrow, dashed, draw=black!40] (ot) -- (buffer);

	\draw [arrow] (enc_t) -- node[above, font=\scriptsize] {$\mathbf{e}_t$} (pred);
	\draw [arrow] (z) -- (pred);
	\draw [arrow] (pred) -- node[above, font=\scriptsize] {$\hat{\mathbf{e}}_{t+1}$} (l2_loss);

	% Bottom path
	\draw[arrow] (otp1) -- (enc_tp1);
	\draw [arrow] (enc_tp1) -| node[pos=0.25, above, font=\scriptsize] {$\mathbf{e}_{t+1}$} (l2_loss);

	% Energy Output Arrow
	\draw[arrow, draw=black!80, line width=1.2pt] (l2_loss.north) -- ++(0,1.5)
	node[above, font=\bfseries] {$E(o_t, o_{t+1}, \tau)$};

\end{tikzpicture}
